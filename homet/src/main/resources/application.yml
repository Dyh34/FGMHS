deepseek:

  api-key:
  url: https://api.deepseek.com/chat/completions
  model: deepseek-chat

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hometeam?serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: 8080
  address: 0.0.0.0

mybatis:
  mapper-locations: classpath:Mapper/*.xml
  type-aliases-package: cn.hjmd.homet.entity
  configuration:
    map-underscore-to-camel-case: true


jwt:
  # 签名密钥
  # token有效期（秒），7天
